services:
  - type: cron
    name: creator-radar-notify
    runtime: node
    branch: main
    buildCommand: bash build.sh
    schedule: "*/30 * * * *"
    startCommand: node worker.js
    envVars:
      - key: APIFY_API_TOKEN
        sync: false
      - key: SLACK_WEBHOOK_URL
        sync: false
      - key: RADAR_NOTIFY_PLATFORMS
        value: reddit,upwork
      # Required for cross-run dedup on Render (Upstash Redis)
      - key: UPSTASH_REDIS_REST_URL
        sync: false
      - key: UPSTASH_REDIS_REST_TOKEN
        sync: false
      - key: SEEN_STORE_REDIS_KEY
        value: creator-radar:seen
      # Required for Upwork on Render — mount session JSON via Secret File
      - key: UPWORK_SESSION_PATH
        sync: false
